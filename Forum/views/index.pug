
doctype html

html(lang="en")

    head

        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")

        link(rel="stylesheet", href="style.css")

        title Retrostan


    body(id="index")
        
        header 
            h1 Welcome to Retrostan
        
        main 
            
            for category in categories 

                button(class="default-button" onclick= `window.location.href = 'http://localhost:4000/category/${category.id}'` )

                    h1= category.name

                    h2= category.desc 
         
        
